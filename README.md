# Лабораторная работа №4

## 📝 Описание
Разработка класса `LineSegment` (отрезок на прямой), демонстрирующего механизмы перегрузки стандартных операторов и приведения типов.

## ✅ Реализованный функционал
- **Унарные операции:**
  - `!s` — вычисление длины отрезка.
  - `++s` — префиксный инкремент (смещение границ на 1).
- **Бинарные операции:**
  - `s + d` — смещение отрезка на целое число.
  - `s > d` — проверка вхождения точки в отрезок.
- **Приведение типов:**
  - Явное (`explicit`) к `int` — получение целой части координаты X.
  - Неявное к `double` — получение координаты Y.
- **Ввод/Вывод:** Перегрузка оператора `<<` для работы с `std::ostream`.

## 🛠 Сборка и запуск
1. Создайте папку для сборки: `mkdir build && cd build`
2. Сконфигурируйте проект: `cmake ..`
3. Соберите проект: `cmake --build .`
4. Запуск: `./Lab2_Executable`
